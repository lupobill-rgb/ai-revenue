-- Drop view (if it exists)
DROP VIEW IF EXISTS v_impressions_clicks_by_workspace;
-- Note: View v_impressions_clicks_by_workspace recreation skipped as required tables may not exist
-- (campaign_channel_stats_daily, channel_spend_daily, cmo_metrics_snapshots)
-- If you need this view, create it manually when all required tables are present.;
